#include <bits\stdc++.h>

using namespace std;

int x[1000];
int main()
{
    int n,d;
    cin >> n >> d;
    for (int i = 0;i<n;i++)
    {
        cin >> x[i].first >> x[i].second;
    }
    sort(x.x+n);

    return 0;
}
